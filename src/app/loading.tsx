/**
 * Global Loading Component
 * 
 * Shows loading state for the entire app.
 * Used in app/loading.tsx for route-level loading.
 */

import { PageLoading } from "@/components/ui/page-loading";

export default function Loading() {
    return <PageLoading />;
}
